---
import { statusLabels, type ProjectStatus } from '../lib/status';

interface Props {
  status: ProjectStatus;
}

const { status } = Astro.props;
---

<span class="border border-rule px-1.5 py-0.5 font-mono text-2xs tracking-label uppercase">
  {statusLabels[status]}
</span>
